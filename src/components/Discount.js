import React, { Component } from 'react'
import DTitle from './DTitle';

export default class Discount extends Component {
  
    render() {
        return (
            <div>
                <DTitle dtitle="discount" />

            </div>
        )
    }
}
